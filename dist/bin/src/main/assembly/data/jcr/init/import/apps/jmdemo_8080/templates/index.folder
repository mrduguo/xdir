<#if (!streamNodeIfIsFile())>
<#include "jquery-mobile.ftl">
</#if>
<#macro page_body>
	<#if (pageProperties["_page_body"]??)>
		${pageProperties["_page_body"].displayableValue}
    <#else>
        Nothing here yet
	</#if>
</#macro>